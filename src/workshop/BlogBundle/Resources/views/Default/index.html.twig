{% extends '::base.html.twig' %}
{% block content %}

{% for post in posts %}
<div class="well">

<article>
	<h2>{{post.title|title}}</h2>
	<p>{{post.text|slice(0,255)}}</p>
	<p>{{post.date|date("m/d/Y")}}</p>
</article>

</div>
{% endfor %}


{% endblock %}